@model IEnumerable<MyTestAplicationMVCEnTity.Models.Pets>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PetsName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PetsDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AnimalsGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PetsName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PetsDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AnimalsGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PetsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PetsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PetsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
